import ProjectPage from "@/components/ProjectPage";
import hitcountDev from "@/projects/2025/hitcount-dev";

export default function ProjectHitCount() {
    return (
        <ProjectPage {...hitcountDev}>
            I use this site on the majority of my projects and I have noticed a handful of others using it too, despite not advertising the site at all.
        </ProjectPage>
    )
}